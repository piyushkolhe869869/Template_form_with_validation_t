import { PanverifyDirective } from './panverify.directive';

describe('PanverifyDirective', () => {
  it('should create an instance', () => {
    const directive = new PanverifyDirective();
    expect(directive).toBeTruthy();
  });
});
